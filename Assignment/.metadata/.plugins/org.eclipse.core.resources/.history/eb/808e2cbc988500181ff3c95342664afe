package MonsoonUmbrellas;
public class Solution {
	static int getUmbrellas(int n, int[] p) {
		int len = p.length;
		int res [] = new int[len];
		int result = 0;
		for(int i=0;i<len;i++)
		{
			if(n%p[i]==0)
			{
				res[i] = n/p[i];
			}
		}
	 return result;
    }
}
